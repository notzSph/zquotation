# PDF exports

This directory is the destination for generated proposal PDFs.

Generated exports are intentionally ignored by Git because they are deliverables, not source files. Keep reusable proposal content in `../json/` and regenerate the PDF when needed.

The directory is committed only to document the export convention and provide a stable destination for local tooling.
